const LOGIN = "/";
const Main = "/main";

const routes = {
    login: LOGIN,
    main: Main,
    // deleteVideo: (id) => {
    //     return id ? `/videos/${id}/delete` : DELETE_VIDEO;
    // },
};

export default routes;
